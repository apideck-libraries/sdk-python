"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .customfield import CustomField, CustomFieldTypedDict
from apideck_unify import models, utils
from apideck_unify.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from apideck_unify.utils import validate_open_enum
from datetime import datetime
from enum import Enum
from pydantic import field_serializer, model_serializer
from pydantic.functional_validators import PlainValidator
from typing import Any, Dict, List, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class ListType(str, Enum, metaclass=utils.OpenEnumMeta):
    r"""Whether the List is static (a fixed set of records) or dynamic (a saved segment that is automatically kept up to date based on filter criteria)."""

    STATIC = "static"
    DYNAMIC = "dynamic"


class ListVisibility(str, Enum, metaclass=utils.OpenEnumMeta):
    r"""The visibility of the List. Which of these values a given connector can return depends on its native sharing model — see the connector-specific gotchas below for details."""

    PRIVATE = "private"
    SHARED = "shared"
    PUBLIC = "public"


class ListTTypedDict(TypedDict):
    id: str
    r"""The unique identifier of the List."""
    name: str
    r"""The name of the List."""
    description: NotRequired[Nullable[str]]
    r"""The description of the List."""
    type: NotRequired[Nullable[ListType]]
    r"""Whether the List is static (a fixed set of records) or dynamic (a saved segment that is automatically kept up to date based on filter criteria)."""
    visibility: NotRequired[Nullable[ListVisibility]]
    r"""The visibility of the List. Which of these values a given connector can return depends on its native sharing model — see the connector-specific gotchas below for details."""
    owner_id: NotRequired[Nullable[str]]
    r"""The unique identifier of the user who owns the List."""
    filter_criteria: NotRequired[Nullable[Dict[str, Any]]]
    r"""The filter criteria used to determine which records belong to a dynamic List. Shape is connector-native and passed through largely as-is (e.g. Close returns its own nested query-tree structure) rather than normalized to a common format."""
    record_count: NotRequired[Nullable[int]]
    r"""The number of records currently in the List."""
    folder_id: NotRequired[Nullable[str]]
    r"""The unique identifier of the folder the List is organized under."""
    object_type: NotRequired[Nullable[str]]
    r"""The type of CRM records the List contains. Normalized to a lowercase plural noun for the vendor's standard object types (e.g. contacts, companies, opportunities, leads, tickets, campaigns, tasks, events); for vendor-specific or custom object types the raw downstream identifier is passed through unchanged, following the connector's native casing (e.g. a HubSpot custom objectTypeId like 2-12345 or a Salesforce custom object API name), so a raw value may not always be distinguishable from a normalized one. This field is intentionally open-ended — no closed enum is enforced."""
    is_dynamic: NotRequired[Nullable[bool]]
    r"""Whether the List is dynamic, meaning its records are automatically kept up to date based on filter criteria. This is a boolean mirror of `type` (`true` when `type` is `dynamic`, `false` when `static`), provided for callers that prefer a boolean check over a string comparison."""
    is_favorite: NotRequired[Nullable[bool]]
    r"""Whether the List has been marked as a favorite."""
    is_default: NotRequired[Nullable[bool]]
    r"""Whether the List is a default List."""
    is_system: NotRequired[Nullable[bool]]
    r"""Whether the List is a system-generated List."""
    custom_fields: NotRequired[List[CustomFieldTypedDict]]
    tags: NotRequired[Nullable[List[str]]]
    updated_by: NotRequired[Nullable[str]]
    r"""The unique identifier of the user who last updated the List."""
    created_by: NotRequired[Nullable[str]]
    r"""The unique identifier of the user who created the List."""
    updated_at: NotRequired[Nullable[datetime]]
    r"""The date and time when the List was last updated."""
    created_at: NotRequired[Nullable[datetime]]
    r"""The date and time when the List was created."""


class ListT(BaseModel):
    id: str
    r"""The unique identifier of the List."""

    name: str
    r"""The name of the List."""

    description: OptionalNullable[str] = UNSET
    r"""The description of the List."""

    type: Annotated[
        OptionalNullable[ListType], PlainValidator(validate_open_enum(False))
    ] = UNSET
    r"""Whether the List is static (a fixed set of records) or dynamic (a saved segment that is automatically kept up to date based on filter criteria)."""

    visibility: Annotated[
        OptionalNullable[ListVisibility], PlainValidator(validate_open_enum(False))
    ] = UNSET
    r"""The visibility of the List. Which of these values a given connector can return depends on its native sharing model — see the connector-specific gotchas below for details."""

    owner_id: OptionalNullable[str] = UNSET
    r"""The unique identifier of the user who owns the List."""

    filter_criteria: OptionalNullable[Dict[str, Any]] = UNSET
    r"""The filter criteria used to determine which records belong to a dynamic List. Shape is connector-native and passed through largely as-is (e.g. Close returns its own nested query-tree structure) rather than normalized to a common format."""

    record_count: OptionalNullable[int] = UNSET
    r"""The number of records currently in the List."""

    folder_id: OptionalNullable[str] = UNSET
    r"""The unique identifier of the folder the List is organized under."""

    object_type: OptionalNullable[str] = UNSET
    r"""The type of CRM records the List contains. Normalized to a lowercase plural noun for the vendor's standard object types (e.g. contacts, companies, opportunities, leads, tickets, campaigns, tasks, events); for vendor-specific or custom object types the raw downstream identifier is passed through unchanged, following the connector's native casing (e.g. a HubSpot custom objectTypeId like 2-12345 or a Salesforce custom object API name), so a raw value may not always be distinguishable from a normalized one. This field is intentionally open-ended — no closed enum is enforced."""

    is_dynamic: OptionalNullable[bool] = UNSET
    r"""Whether the List is dynamic, meaning its records are automatically kept up to date based on filter criteria. This is a boolean mirror of `type` (`true` when `type` is `dynamic`, `false` when `static`), provided for callers that prefer a boolean check over a string comparison."""

    is_favorite: OptionalNullable[bool] = UNSET
    r"""Whether the List has been marked as a favorite."""

    is_default: OptionalNullable[bool] = UNSET
    r"""Whether the List is a default List."""

    is_system: OptionalNullable[bool] = UNSET
    r"""Whether the List is a system-generated List."""

    custom_fields: Optional[List[CustomField]] = None

    tags: OptionalNullable[List[str]] = UNSET

    updated_by: OptionalNullable[str] = UNSET
    r"""The unique identifier of the user who last updated the List."""

    created_by: OptionalNullable[str] = UNSET
    r"""The unique identifier of the user who created the List."""

    updated_at: OptionalNullable[datetime] = UNSET
    r"""The date and time when the List was last updated."""

    created_at: OptionalNullable[datetime] = UNSET
    r"""The date and time when the List was created."""

    @field_serializer("type")
    def serialize_type(self, value):
        if isinstance(value, str):
            try:
                return models.ListType(value)
            except ValueError:
                return value
        return value

    @field_serializer("visibility")
    def serialize_visibility(self, value):
        if isinstance(value, str):
            try:
                return models.ListVisibility(value)
            except ValueError:
                return value
        return value

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = [
            "description",
            "type",
            "visibility",
            "owner_id",
            "filter_criteria",
            "record_count",
            "folder_id",
            "object_type",
            "is_dynamic",
            "is_favorite",
            "is_default",
            "is_system",
            "custom_fields",
            "tags",
            "updated_by",
            "created_by",
            "updated_at",
            "created_at",
        ]
        nullable_fields = [
            "description",
            "type",
            "visibility",
            "owner_id",
            "filter_criteria",
            "record_count",
            "folder_id",
            "object_type",
            "is_dynamic",
            "is_favorite",
            "is_default",
            "is_system",
            "tags",
            "updated_by",
            "created_by",
            "updated_at",
            "created_at",
        ]
        null_default_fields = []

        serialized = handler(self)

        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)
            serialized.pop(k, None)

            optional_nullable = k in optional_fields and k in nullable_fields
            is_set = (
                self.__pydantic_fields_set__.intersection({n})
                or k in null_default_fields
            )  # pylint: disable=no-member

            if val is not None and val != UNSET_SENTINEL:
                m[k] = val
            elif val != UNSET_SENTINEL and (
                not k in optional_fields or (optional_nullable and is_set)
            ):
                m[k] = val

        return m
