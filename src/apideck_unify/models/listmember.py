"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from apideck_unify.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from pydantic import model_serializer
from typing_extensions import NotRequired, TypedDict


class ListMemberTypedDict(TypedDict):
    id: str
    r"""The unique identifier of the record that belongs to the List, as returned by the downstream connector."""
    object_type: NotRequired[Nullable[str]]
    r"""The type of CRM record this member is. Normalized to a lowercase plural noun for the vendor's standard object types (e.g. contacts, companies, opportunities, leads, tickets, campaigns, tasks, events); for vendor-specific or custom object types the raw downstream identifier is passed through unchanged, mirroring the semantics of `List.object_type`. This field is intentionally open-ended — no closed enum is enforced."""


class ListMember(BaseModel):
    id: str
    r"""The unique identifier of the record that belongs to the List, as returned by the downstream connector."""

    object_type: OptionalNullable[str] = UNSET
    r"""The type of CRM record this member is. Normalized to a lowercase plural noun for the vendor's standard object types (e.g. contacts, companies, opportunities, leads, tickets, campaigns, tasks, events); for vendor-specific or custom object types the raw downstream identifier is passed through unchanged, mirroring the semantics of `List.object_type`. This field is intentionally open-ended — no closed enum is enforced."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = ["object_type"]
        nullable_fields = ["object_type"]
        null_default_fields = []

        serialized = handler(self)

        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)
            serialized.pop(k, None)

            optional_nullable = k in optional_fields and k in nullable_fields
            is_set = (
                self.__pydantic_fields_set__.intersection({n})
                or k in null_default_fields
            )  # pylint: disable=no-member

            if val is not None and val != UNSET_SENTINEL:
                m[k] = val
            elif val != UNSET_SENTINEL and (
                not k in optional_fields or (optional_nullable and is_set)
            ):
                m[k] = val

        return m
